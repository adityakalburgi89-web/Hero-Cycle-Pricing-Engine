{
  "name": "hero-cycle-pricing-engine",
  "version": "1.0.0",
  "description": "Full-stack cycle pricing engine with time-sensitive pricing",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test tests/*.test.js",
    "seed": "node backend/config/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongoose": "^8.5.0"
  }
}
