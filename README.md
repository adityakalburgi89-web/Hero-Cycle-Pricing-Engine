# Hero Cycle Pricing Engine

A pricing calculator for bicycle parts with region and material multipliers.

## Structure

- `backend/` - Express API with pricing calculation logic
- `frontend/` - Vanilla JS client for the pricing UI
- `wireframes/` - Design mockups

## Quickstart

```bash
cd backend
npm install
npm start
```

Open `frontend/index.html` in a browser.
